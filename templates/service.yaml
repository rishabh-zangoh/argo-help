apiVersion: v1
kind: Service
metadata:
  name: {{ include "llama-model.fullname" . }}
  labels:
    {{- include "llama-model.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "llama-model.selectorLabels" . | nindent 4 }}
